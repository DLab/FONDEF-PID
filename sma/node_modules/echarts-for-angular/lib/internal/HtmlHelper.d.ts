import { Observable } from 'rxjs';
export declare class HtmlHelper {
    static elementIsFocusable(ele: HTMLElement): boolean;
    static getWidthSensor(element: HTMLElement, periodicityInMiliSeconds?: number): Observable<number>;
    static getHeightSensor(element: HTMLElement, periodicityInMiliSeconds?: number): Observable<number>;
}
